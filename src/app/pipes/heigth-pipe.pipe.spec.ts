import { HeigthPipePipe } from './heigth-pipe.pipe';

describe('HeigthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeigthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
